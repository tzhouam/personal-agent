# secrets — never commit
.env

# python
.venv/
venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
build/
dist/

# local artifacts
nohup.out
*.log
.deployed-commit

# WeChat login QR — a live credential; must NEVER be committed (served in-memory
# over loopback /qr; this is a backstop if one is ever written under the repo)
qr-login.*
*-qr.png

# PA-Mix results (raw model outputs; doc/BENCHMARKS.md §2.7)
bench/results/

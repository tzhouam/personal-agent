"""User / agent layer: one owner's personal agent.

The daily pipeline (per-owner), the profile/health/finance/todo stores, chat,
research, website, deliver. Agent code may import `assistant.platform.*`; the
reverse is forbidden and enforced by `test/test_boundary.py`."""

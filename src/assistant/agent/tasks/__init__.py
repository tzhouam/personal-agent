"""Task package: the per-run work units the orchestrator invokes — profile
update and weekly consolidation, GitHub notification digest, todo derivation,
dormancy curation, and resume sync. Each submodule owns one task."""
